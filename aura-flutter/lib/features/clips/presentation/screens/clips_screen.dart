import 'package:aura/features/clips/presentation/screens/clips_reel_screen.dart';
import 'package:flutter/material.dart';

/// Entry point screen for the Clips feature tab.
class ClipsScreen extends StatelessWidget {
  const ClipsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const ClipsReelScreen();
  }
}
